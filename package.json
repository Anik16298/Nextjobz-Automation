{
    "name": "nextjobz-automation",
    "version": "1.0.0",
    "description": "Nextjobz Playwright UI Automation Framework",
    "type": "module",
    "scripts": {
        "test": "npx playwright test && npx allure generate allure-results --clean -o allure-report && npx playwright show-report",
        "test:allure": "npx playwright test && npx allure generate allure-results --clean -o allure-report",
        "test:playwright": "npx playwright test && npx playwright show-report",
        "allure:open": "npx allure open allure-report",
        "allure:generate": "npx allure generate allure-results --clean -o allure-report"
    },
    "devDependencies": {
        "@playwright/test": "^1.40.0",
        "@types/node": "^25.5.0",
        "allure-commandline": "^2.36.0",
        "allure-playwright": "^3.4.5",
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "dotenv": "^17.2.4"
    }
}
